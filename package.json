{
  "private": true,
  "name": "graphql-shield",
  "author": "SiteMinder",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "if [ ! -f ./node_modules/.bin/playpen ]; then (cd node_modules/.bin; ln -f -s ../../.buildkite/node_modules/.bin/playpen .; ln -f -s ../../.buildkite/node_modules/.bin/pen .); fi",
    "overlord:install": "./.buildkite/install-overlord.sh"
  },
  "devDependencies": {
    "@siteminder/base-typescript": "^0.7.1",
    "@siteminder/eslint-config": "^5.4.0",
    "@siteminder/testing": "^5.3.2",
    "@types/node": "^20.19.4"
  }
}
