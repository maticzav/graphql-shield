import { IResolvers, IResolverObject, IPermissions, IPermissionResolver } from './types'

export default (resolvers: IResolvers, permissions: IPermissions): IResolvers => {

}

function mergeDeep() {

}

function resolvePermission(resolver: IResolverObject, permission: IPermissionResolver): Promise<IResolverObject> {
   
}